import { StudentCourseDTO } from './student-course-dto';

describe('StudentCourseDTO', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new StudentCourseDTO()).toBeTruthy();
  });
});
